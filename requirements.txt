firebase-admin>=6.5.0
# Blocco: le release del 24/08/2026 rompono il database id "(default)"
# -> errore "400 Invalid database id %28default%29". Il colpevole verificato
# e' google-api-core 2.35.0 (la codifica percent dell'header di routing);
# google-cloud-firestore 2.29.0, uscita lo stesso giorno, resta bloccata per
# prudenza. Sbloccare solo dopo verifica.
google-cloud-firestore==2.28.1
google-api-core==2.34.0
